import 'package:flutter/material.dart';

class WLimit extends StatelessWidget {
  const WLimit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Limit"));
  }
}
