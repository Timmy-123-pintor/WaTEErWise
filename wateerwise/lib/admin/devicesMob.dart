import 'package:flutter/material.dart';

class DevicesMob extends StatelessWidget {
  const DevicesMob({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("DevicesMob"),
    );
  }
}
