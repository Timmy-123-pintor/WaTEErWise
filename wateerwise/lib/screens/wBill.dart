import 'package:flutter/material.dart';

class WBill extends StatelessWidget {
  const WBill({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Bill"));
  }
}
